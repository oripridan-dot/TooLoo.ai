services:
  - type: web
    name: tooloo-ai
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node servers/web-server-lite.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
